@model WebApi.Models.Directory

<div ng-app="dirViewApp">
    <div class="container" ng-controller="dir_view">
        <table class="col-md-4 table table-bordered table-condensed" >
            <thead>
                <tr>
                    <th>Less 10Mb</th>
                    <th>10Mb-50Mb</th>
                    <th>More 100Mb</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>{{ dir.SmallFilesCount }}</td>
                    <td>{{ dir.MediumFilesCount }}</td>
                    <td>{{ dir.LargeFilesCount }}</td>
                </tr>
            </tbody>
        </table>
        <input ng-model="path" ></input>
        <button ng-click="open()">open</button>
        <p class="current-path">
            Current path: <span class="path">{{ dir.Path }}</span>
        </p>
        <ul>
            <li ng-repeat="file in dir.Directories"><a href="#" ng-click="open(file.Path)">{{ file.Name }}</a></li>
            <li ng-repeat="file in dir.Files"><a href="#" ng-click="open(file.Path)">{{ file.Name }}</a></li>
        </ul>
    </div>
</div>
@section Scripts{
    @Scripts.Render("~/bundles/angular")    
    @Scripts.Render("~/bundles/angular-resource")
    @Scripts.Render("~/bundles/dir_view")
}
